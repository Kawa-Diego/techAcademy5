import type { ReactElement } from 'react';
import { AboutSection } from '../../components/AboutSection';

export const LandingSobre = (): ReactElement => (
  <main className="min-h-screen px-4 pb-20 pt-24 sm:px-6 lg:pt-28">
    <AboutSection />
  </main>
);
